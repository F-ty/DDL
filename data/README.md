# Data

This directory stores FashionIQ, Shoes, Fashion200k, and pretrained CLIP
weights in the layout expected by `src/train.py` and `src/eval.py`.

Large binary/image files are intended to be tracked with Git LFS when this
package is uploaded to GitHub.
